{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Program",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/kepler.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build",
            "stopAtEntry": false,
            "sourceFileMap": {
                "/usr/src/app": "${workspaceFolder}"
            }
        }
    ]
}